@extends('layouts.app')
@section('title', 'Generar bitácora')
@section('content')
<div class="mb-3"><h1 class="h3 mb-1">Generar bitácora</h1><p class="text-body-secondary mb-0">Selecciona un período para descargar los eventos de auditoría en CSV. El sistema conserva los registros y no permite períodos solapados.</p></div>
<form method="POST" action="{{ route('audit-logs.generate') }}" class="card mb-4"><div class="card-body"><div class="row g-3"><div class="col-md-4"><label class="form-label" for="desde">Desde</label><input id="desde" type="date" name="desde" value="{{ old('desde', now()->startOfMonth()->toDateString()) }}" class="form-control" required>@error('desde')<div class="text-danger small">{{ $message }}</div>@enderror</div><div class="col-md-4"><label class="form-label" for="hasta">Hasta</label><input id="hasta" type="date" name="hasta" value="{{ old('hasta', now()->toDateString()) }}" class="form-control" required>@error('hasta')<div class="text-danger small">{{ $message }}</div>@enderror</div><div class="col-md-2"><label class="form-label" for="formato">Formato</label><select id="formato" name="formato" class="form-select"><option value="pdf" @selected(old('formato') === 'pdf')>PDF</option><option value="csv" @selected(old('formato') === 'csv')>CSV</option></select></div><div class="col-md-2 d-flex align-items-end"><button class="btn btn-primary w-100">Generar</button></div></div></div></form>
<h2 class="h5">Bitácoras generadas</h2><div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Período UTC</th><th>Archivo</th><th>Integridad SHA-256</th><th>Generada</th></tr></thead><tbody>@forelse($archives as $archive)<tr><td>{{ \Illuminate\Support\Carbon::parse($archive->periodo_desde)->format('d-m-Y H:i') }} — {{ \Illuminate\Support\Carbon::parse($archive->periodo_hasta)->format('d-m-Y H:i') }}</td><td>{{ basename($archive->ruta) }}</td><td><code>{{ $archive->sha256 }}</code></td><td>{{ \Illuminate\Support\Carbon::parse($archive->creado_at)->format('d-m-Y H:i') }}</td></tr>@empty<tr><td colspan="4" class="text-center text-body-secondary p-4">Aún no se han generado bitácoras.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $archives->links() }}</div>
@endsection
