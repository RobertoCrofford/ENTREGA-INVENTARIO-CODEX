<!doctype html>
<html lang="es"><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans,sans-serif;font-size:8px;color:#222}h1{font-size:16px;margin:0 0 4px}p{margin:0 0 12px;color:#555}table{width:100%;border-collapse:collapse}th,td{border:1px solid #bbb;padding:4px;vertical-align:top}th{background:#e9ecef;text-align:left}.muted{color:#666;word-break:break-word}</style></head><body>
<h1>Bitácora de auditoría</h1><p>Período UTC: {{ $from->format('d-m-Y H:i') }} — {{ $to->format('d-m-Y H:i') }} · Eventos: {{ $entries->count() }}</p>
<table><thead><tr><th>Fecha UTC</th><th>Usuario</th><th>Acción</th><th>Entidad</th><th>Razón</th><th>Resultado / IP</th><th>Cambios</th></tr></thead><tbody>@forelse($entries as $entry)<tr><td>{{ $entry->creado_at }}</td><td>{{ $entry->usuario_nombre ? $entry->usuario_nombre.' ('.$entry->usuario_username.')' : 'Sistema' }}</td><td>{{ $entry->accion }}</td><td>{{ $entry->entidad_tipo }} #{{ $entry->entidad_id }}</td><td>{{ $entry->motivo }}</td><td>{{ $entry->resultado }}<br>{{ $entry->ip }}</td><td class="muted">Antes: {{ $entry->antes_json }}<br>Después: {{ $entry->despues_json }}</td></tr>@empty<tr><td colspan="7">No hay eventos en el período seleccionado.</td></tr>@endforelse</tbody></table>
</body></html>
