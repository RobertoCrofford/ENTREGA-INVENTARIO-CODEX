@extends('layouts.app')
@section('title', 'Usuarios')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="h3 mb-0">Usuarios</h1><p class="text-body-secondary mb-0">Administración de cuentas y roles fijos.</p></div><a class="btn btn-primary" href="{{ route('users.create') }}">Crear usuario</a></div>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Nombre</th><th>Usuario</th><th>Rol</th><th>Estado</th><th>Contraseña</th><th></th></tr></thead><tbody>@forelse($users as $user)<tr><td>{{ $user->name }}<div class="small text-body-secondary">{{ $user->email }}</div></td><td>{{ $user->username }}</td><td>{{ $user->rol->nombre }}</td><td><span class="badge text-bg-{{ $user->activo ? 'success' : 'secondary' }}">{{ $user->activo ? 'Activo' : 'Inactivo' }}</span></td><td>{{ $user->debe_cambiar_password ? 'Pendiente de cambio' : 'Configurada' }}@if($user->password_cambiado_at)<div class="small text-body-secondary">{{ $user->password_cambiado_at->format('d-m-Y H:i') }}</div>@endif</td><td class="text-end"><a href="{{ route('users.edit', $user) }}" class="btn btn-sm btn-outline-primary">Editar</a>@if(auth()->user()->tieneRol(\App\Models\Role::SUPERADMIN))<details class="d-inline-block ms-1 text-start"><summary class="btn btn-sm btn-outline-warning">Restablecer</summary><form class="card card-body position-absolute end-0 mt-1 shadow" style="width: 360px; z-index: 5" method="POST" action="{{ route('users.reset-password', $user) }}">@csrf @method('PUT')<div class="input-group input-group-sm mb-2"><input class="form-control" type="password" id="reset_password_{{ $user->id }}" name="password" placeholder="Contraseña temporal (12+ caracteres)" required><button class="btn btn-outline-secondary" type="button" data-password-toggle aria-controls="reset_password_{{ $user->id }}" aria-label="Mostrar contraseña" title="Mostrar contraseña"><i class="bi bi-eye" aria-hidden="true"></i></button></div><div class="input-group input-group-sm mb-2"><input class="form-control" type="password" id="reset_password_confirmation_{{ $user->id }}" name="password_confirmation" placeholder="Confirmar contraseña" required><button class="btn btn-outline-secondary" type="button" data-password-toggle aria-controls="reset_password_confirmation_{{ $user->id }}" aria-label="Mostrar contraseña" title="Mostrar contraseña"><i class="bi bi-eye" aria-hidden="true"></i></button></div><button class="btn btn-sm btn-warning">Restablecer contraseña</button></form></details>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-body-secondary py-4">No hay usuarios creados.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $users->links() }}</div>
@endsection
